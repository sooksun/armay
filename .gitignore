# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local
# production env holds the DB password + JWT secret — never commit
.env.production

# typescript
*.tsbuildinfo
next-env.d.ts

# uploads (local disk attachments)
/uploads/*
!/uploads/.gitkeep

# git worktrees (local dev isolation, not part of the repo)
/.worktrees/
